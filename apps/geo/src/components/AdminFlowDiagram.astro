---
/**
 * @deprecated Use HistoricalFlowCard instead. Kept for backward compatibility.
 */
import HistoricalFlowCard from "@/components/HistoricalFlowCard.astro";
import type { AdministrativeFlow } from "@/lib/historical-flow";

interface Props {
  flow: AdministrativeFlow;
  title?: string;
}

const { flow, title } = Astro.props;
---

<HistoricalFlowCard flow={flow} title={title} />
