/** Default number of rows shown before "Vis alle" on data lists. */
export const INITIAL_LIST_SIZE = 20;

/** Larger initial cap for global search index pages (skoler / barnehager). */
export const INITIAL_SEARCH_LIST_SIZE = 100;
