{
  "name": "@aurii/core",
  "version": "0.1.0",
  "description": "Aurii Core — Phase 1: Import-first Runtime",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "cli": "bun run src/cli.ts",
    "serve": "bun run src/api/server.ts",
    "dev": "bun --watch src/api/server.ts"
  },
  "dependencies": {
    "yaml": "^2.7.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
