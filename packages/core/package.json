{
  "name": "@aurii/core",
  "version": "0.3.0",
  "description": "Aurii Core — Declarative Runtime for Structured Knowledge",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "cli": "bun run src/cli.ts",
    "serve": "bun run src/api/server.ts",
    "dev": "bun --watch src/api/server.ts",
    "build": "bun run typecheck",
    "test": "bun test src/__tests__",
    "test:watch": "bun test --watch src/__tests__",
    "typecheck": "tsc --noEmit",
    "lint": "bunx @biomejs/biome lint src/"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.4.2",
    "@elysiajs/swagger": "^1.3.1",
    "elysia": "^1.4.29",
    "yaml": "^2.7.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^6.0.3"
  }
}
